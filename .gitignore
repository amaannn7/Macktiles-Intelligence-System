# Dev junk
error_log
.DS_Store
.claude/

# Runtime-generated files (auto-recreate — don't ship)
data/.rate_limits.json
data/chat_last_read_*.json
data/uploads/*
!data/uploads/.gitkeep

# Note: users.json, admin.json, user_*.json, channels.json, and
# messages_*.json ARE committed so the live server starts with the
# current accounts, leads, and chat. Repo is private.
