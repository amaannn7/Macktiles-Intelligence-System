# Dev junk
error_log
.DS_Store
.claude/

# Runtime-generated files (auto-recreate — don't ship)
data/.rate_limits.json
data/chat_last_read_*.json
data/uploads/*
!data/uploads/.gitkeep

# Sensitive data — NEVER commit (contains API keys, passwords, lead data)
data/admin.json
# data/users.json
data/user_*.json
